import { CropWordsPipe } from './crop-words.pipe';

describe('CropWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new CropWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
